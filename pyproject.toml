[project]
name = "larkspur"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tryproxy", email = "tryproxy@protonmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "django==6.1.1",
]

[dependency-groups]
dev = [
    "ruff",
]

[project.scripts]
larkspur = "larkspur:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
